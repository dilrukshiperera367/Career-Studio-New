from django.apps import AppConfig


class MarketplaceMessagingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.marketplace_messaging"
    verbose_name = "Marketplace Messaging"
